import { View, StyleSheet } from "react-native";

const TaskModalViewMode = () => {
  return <View style={[styles.TaskModalViewMode]}></View>;
};

const styles = StyleSheet.create({
  TaskModalViewMode: {},
});

export default TaskModalViewMode;
