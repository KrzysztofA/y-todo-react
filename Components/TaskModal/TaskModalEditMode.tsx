import { View, StyleSheet } from "react-native";

const TaskModalEditMode = () => {
  return <View style={[styles.TaskModalEditMode]}></View>;
};

const styles = StyleSheet.create({
  TaskModalEditMode: {},
});

export default TaskModalEditMode;
